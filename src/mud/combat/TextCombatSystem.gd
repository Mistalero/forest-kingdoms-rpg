# TextCombatSystem.gd
# Класс для текстовой системы боя в MUD режиме

extends Node

# Начало боя
func start_combat(enemy_name: String):
	print("Вы сталкиваетесь с " + enemy_name + "!")
	# Здесь должна быть логика инициализации боя
	# Например, получение ссылок на игрока и врага
	# и установка начальных параметров боя
	_render_combat_status()

# Обработка атаки игрока
func player_attack(attack_type: String):
	# Здесь должна быть логика обработки атаки игрока
	# Например, расчет урона, применение эффектов и т.д.
	
	var damage = 8
	var enemy_name = "лесной волк"
	
	print("Вы наносите " + enemy_name + " " + str(damage) + " урона!")
	
	# Здесь должна быть логика проверки, жив ли враг
	# и завершения боя при необходимости
	
	_enemy_attack()

# Обработка атаки врага
func _enemy_attack():
	# Здесь должна быть логика обработки атаки врага
	# Например, расчет урона, применение эффектов и т.д.
	
	var damage = 3
	var enemy_name = "лесной волк"
	
	print(enemy_name + " наносит вам " + str(damage) + " урона!")
	
	_render_combat_status()

# Отображение статуса боя
func _render_combat_status():
	# Здесь должна быть логика получения текущего состояния боя
	# и отображения его текстового представления
	
	var enemy_name = "лесной волк"
	var enemy_health = 12
	var enemy_max_health = 20
	var player_health = 47
	var player_max_health = 50
	
	print(enemy_name + ": Здоровье " + str(enemy_health) + "/" + str(enemy_max_health))
	print("Вы: Здоровье " + str(player_health) + "/" + str(player_max_health))
	print("")

# Попытка убежать из боя
func attempt_flee():
	# Здесь должна быть логика попытки убежать
	# Например, проверка случайного значения и завершение боя при успехе
	
	print("Вы пытаетесь убежать...")
	
	# Для примера, всегда успешно убегаем
	print("Вы успешно убежали от боя!")
	
	# Здесь должна быть логика завершения боя
	# и возврата к нормальной игре

# Использование заклинания в бою
func cast_spell(spell_name: String):
	# Здесь должна быть логика использования заклинания
	# Например, проверка наличия заклинания у игрока,
	# расчет эффекта и применение его
	
	print("Вы произносите заклинание " + spell_name + "!")
	
	# Для примера, наносим урон врагу
	var damage = 15
	var enemy_name = "лесной волк"
	
	print("Заклинание наносит " + enemy_name + " " + str(damage) + " урона!")
	
	# Здесь должна быть логика проверки, жив ли враг
	# и завершения боя при необходимости
	
	_enemy_attack()